# 🛍️ E-Commerce Platform API (Laravel)

Проект интернет-магазина с полным циклом обработки заказов, написанный на Laravel.

## 🚀 Возможности

### 🔐 Аутентификация и пользователи
- Регистрация и авторизация (Sanctum) с возможность refresh.
- Выход с инвалидацией токенов
- Система ролей и групп пользователей
- Несколько профилей компаний на одного пользователя

### 🏪 Каталог товаров
- Полнотекстовый поиск товаров (Elasticsearch)
- Сортировка по цене, дате, кол-ву на складе
- Несколько видов цен (розничная, оптовая, специальная)
- Учет остатков на нескольких складах
- Галерея изображений товаров

### 📦 Корзина и заказы
- Корзина для авторизованных и анонимных пользователей
- Расчет итоговой стоимости с учетом объема заказа
- Полный цикл оформления заказа
- История заказов с фильтрацией
- Повторение заказов
- Изменение кол-ва товаров на складе при оформлении и отмене заказа

### 📰 Система новостей
- Блог с тегами
- Сортировка и фильтрация
- Медиа-галерея для новостей

### 🖼️ Медиа менеджер
- Загрузка и оптимизация изображений
- Автоматическое создание превью разных размеров
- Привязка медиа к разным моделям (товары, новости, пользователи)

## 🔧 Особенности реализации

- **RESTful API** со стандартизированными ответами
- **Laravel Sanctum** для аутентификации
- **DTO** для передачи данных между слоями
- **Form Requests** для валидации
- **API Resources** для трансформации ответов
- **OpenAPI** документация

## 📊 Основные сущности

- **Пользователи** - система аккаунтов с ролями
- **Товары** - каталог с ценами и остатками
- **Заказы** - полный цикл покупки
- **Новости** - контентная часть платформы

## 🛠 Технологический стек

- **Backend**: Laravel 11+, PHP 8.1+
- **База данных**: PostgreSQL 16
- **Поиск**: Elasticsearch 8.11 + Kibana
- **Кэширование**: Redis
- **Очереди**: RabbitMQ
- **Веб-сервер**: Nginx + PHP-FPM
- **Аутентификация**: Laravel Sanctum
- **Документация API**: OpenAPI 3.0



## Docker compose сборка взята с [репозитория](https://github.com/rw4lll/laravel-docker-examples.git).


## 🐳 Запуск проекта (Docker)

### Требования:
- Docker
- Docker Compose

1. Скопируйте файл .env.example в .env и настройте необходимые переменные окружения:

```bash
cp .env.example .env
```

Подсказка: настройте переменные `UID` и `GID` в файле `.env` в соответствии с вашим ID пользователя и группы. Вы можете найти их выполнив `id -u` и `id -g` в терминале.

2. Запустите сервисы Docker Compose:

```bash
docker compose -f compose.dev.yaml up -d
```

3. Установите зависимости Laravel:

```bash
docker compose -f compose.dev.yaml exec workspace bash
composer install
php artisan key:generate
npm install
npm run dev
```

4. Запустите заново сборку поднятие контейнеров, чтобы поднять php 

```bash
docker compose -f compose.dev.yaml up -d
```

5. Запустите миграции:

```bash
docker compose -f compose.dev.yaml exec workspace php artisan migrate
```
```bash
docker compose -f compose.dev.yaml exec workspace php artisan elastic:migrate
```

6. Доступ к приложению:

Откройте браузер и перейдите по [http://localhost](http://localhost).
Swagger документация доступна по адресу [http://localhost/api/documentation](http://localhost/api/documentation)

7. Запуск очередей (опционально):

```bash
docker-compose exec workspace php artisan queue:work
```

## 🧪 Тестирование

1. Для локального тестирования можно сгенерировать тестовые данные:

```bash
docker compose -f compose.dev.yaml exec workspace php artisan db:seed
```

**Покрыты основные сценарии:**

- **🔐 Авторизация** - регистрация, вход, выход, обновление токенов
- **🛒 Корзина** - добавление/удаление товаров, расчет стоимости
- **🔍 Поиск** - работа поискового сервиса

**Запуск тестов:**
```bash
php artisan test
```

### Сервисы и порты:

- **Приложение**: http://localhost:80

- **PostgreSQL**: localhost:5433

- **Redis**: localhost:6379

- **RabbitMQ Management**: http://localhost:15673

- **Elasticsearch**: http://localhost:9201

- **Kibana**: http://localhost:5602

- **Vite Dev Server**: http://localhost:5174

